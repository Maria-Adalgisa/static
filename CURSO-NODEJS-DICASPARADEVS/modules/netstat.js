/*const netstat = require("netstat");
const netstat = require("node-netstat");

netstat({
  filter: {
    state: "LISTEN",
    protocol: "tcp",
  },
  done: (data) => {
    console.log(data.local.port);
  },
});*/
